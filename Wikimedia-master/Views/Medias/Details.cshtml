@model Models.Media
@{ ViewBag.PageTitle = "Détails"; }

<div id="detailsPanel" class="detailsPanel"></div>

@section Scripts {
    <script>

        let mediaDetails_ARP = null;
        $(() => {
            mediaDetails_ARP = new AutoRefreshedPanel("detailsPanel", "/Medias/GetMediaDetails", 5);
        })
    </script>
}